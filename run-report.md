# flippy-bird — run report

Measured agent build run, Aug 12 2026. Agent under test: `opencode/deepseek-v4-flash-free`.

## Metrics

Source: `opencode.db` `session` table (`opencode db`), `parent_id IS NULL`, `path='Users/Shared/llm_workspace/flippy-bird'`.

| Metric | Value |
|---|---|
| Input tokens | 61,024 |
| Output tokens | 29,695 |
| Wall-clock | 222.0 s |
| Cost | $0.00 (free gateway) |

## What was built

- `index.html`, `styles.css`, `game.js` (Canvas 2D demo, `window.__flippy` test hooks, procedural WebAudio, localStorage best/mute, PWA registration).
- `manifest.webmanifest`, `sw.js` (cache-first app shell).
- `icon-192.png`, `icon-512.png` — generated by `scripts/make-icons.mjs` (pure-node PNG encoder, geometric bird; no browser canvas on the build machine, so the 🐤 emoji render path was replaced per spec §8).
- `scripts/check-logic.mjs` — headless node verification harness.

## Agent-side verification (node-logic checks)

Per the re-plan, **all browser-level testing was dropped** (Chromium dead on this machine; safaridriver lacks touch/console/offline emulation and is token-expensive). The agent run verifies game core logic and PWA artifacts headlessly; the user verifies real-browser feel (render, sound, touch/click/space, zero console errors, offline-from-cache) on the deployed URL.

`node scripts/make-icons.mjs` and `node scripts/check-logic.mjs`:

| Check | Result |
|---|---|
| game.js boots in sandbox without throwing | PASS |
| initial state is title | PASS |
| mute toggle visible on title | PASS |
| pointerdown starts the game | PASS |
| mute toggle hidden during play | PASS |
| space bar flaps the bird (birdY decreases) | PASS |
| score increments as a pipe passes | PASS |
| fatal collision leads to game-over | PASS |
| best reflects score on game over | PASS |
| mute toggle visible on game over | PASS |
| mute toggle flips muted state | PASS |
| mute toggle persisted to localStorage | PASS |
| best score persists across reload | PASS |
| mute state persists across reload | PASS |
| manifest references both icons | PASS |
| both icon files exist and are non-empty | PASS |
| sw.js precaches the full app shell | PASS |
| index.html links manifest, registers sw, loads game | PASS |
| mute button and canvas are the only DOM composite elements | PASS |

PWA artifacts: `icon-192.png` (192×192, RGBA) and `icon-512.png` (512×512, RGBA) present.

## User-side verification (on the deployed URL)

Pending ticket **04-Deploy-to-URL**. After deploy, verify on real desktop and phone:
- Title → play → game-over with click/space (desktop) and touch (phone); zero console errors.
- Bird flips, flaps, scores, dies per physics; sound + mute toggle persistent; best score persists; loads from cache with network disabled.

## Spec deviations

- Icon render path: geometric bird (pure-node) instead of 🐤 emoji canvas render — no browser canvas available at build time (spec §8 amended).
- Verification distribution: browser-level checks moved from agent to user (spec §9 amended).